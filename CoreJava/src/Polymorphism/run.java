package Polymorphism;

public class run {
    public static void main (String args[]){
        saving s = new saving();
        s.balance(1000);

        current c = new current();
        c.balance(2000);
    }
}

